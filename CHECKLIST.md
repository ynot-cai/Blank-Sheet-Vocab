# 上线自检清单

部署完照着勾一遍。**勾不上就说明有问题**，别跳过去——这份清单里的每一条都对应一个真会踩的坑。

> 自动化部分已经写成脚本：`npm test`（本地跑，需要 0 个云端账号）会跑完
> 后端 58 项 + 同步 43 项 + AI 代理 53 项 + 移动端 48 项 + PWA 49 项。
> 下面的清单是**自动化测不到**的部分（真机手感、跨设备、视觉）。

---

## 一、功能自检（电脑上）

- [ ] 首页 6 个入口都能进（录入 / 背诵 / 记忆 / 单词列表 / 复习 / 设置）
- [ ] 录入 → 解析 → 合并 → 入库，全流程能走通（规则解析也行，不填密钥也能用）
- [ ] 背诵：单词能出现、朗读正常、点单词出/收中文意思、点中文意思弹单词卡
- [ ] 「再背一个」点 3 次后按钮变「记忆」；「再次记忆」能立即开始一次记忆
- [ ] 「保存并退出」能保存位置与每词记忆次数；重进能恢复
- [ ] 「背完了」在每词记忆达标后出现（红色），点击能归档
- [ ] 记忆：默写 → 判分 → 答案卡「点卡片 / 点白纸 / 空格 / Enter」四种都能继续
- [ ] 复习：能分组、能给出推荐数量
- [ ] 列表：搜索、筛选、改属性、斩/复活、导出导入都能用
- [ ] 设置改动即时生效（改字号能立刻在白纸上看到）

## 二、云同步自检

- [ ] 设置 → F 区填后端地址 + 同步码 → 「测试连接」是绿字
- [ ] 「立即同步」后，云端确实有数据（浏览器打开 `/api/health` 是 `db: connected`）
- [ ] 多设备：手机与电脑填**同一个同步码** → 能看到同一份词库
- [ ] 隔离：换一个同步码 → 看不到前一个码的数据
- [ ] 增量：电脑上改一个词，等几秒，手机上同步能看到变化
- [ ] 断网：关掉 WiFi → 应用照常打开、照常背单词，顶部只有一条轻提示
- [ ] 同步失败连续 3 次后，顶部提示条变成常驻，但**不影响任何操作**

## 三、多端自检（真机，必须动手点）

- [ ] 手机上：右下角 4 个按钮竖排、**都不置灰**、都能点
- [ ] 手机上：单词后面有浅灰义项序号（**只有 1 个义项的词不显示**）
- [ ] 手机上：单词**没有**发音按钮、**没有** hover 工具条
- [ ] 手机上：撒满一屏后，右下角按钮区**没有**压住单词
- [ ] 手机上：点单词出中文意思、再点收起；点中文意思弹出卡片（底部弹出）
- [ ] 平板上：以上同样成立，布局不别扭
- [ ] 手机上：输入框聚焦时页面**不会**被自动放大（字号 ≥16px 生效）
- [ ] 手机上：所有可点元素热区 ≥44×44px（手指点得中）

## 四、PWA 自检

- [ ] Chrome DevTools → Application → Manifest：无错误，图标能显示
- [ ] Service Worker 面板：状态是 activated
- [ ] DevTools → Network 勾 Offline → 刷新 → **应用能打开**（不是浏览器错误页）
- [ ] 断网时能进背诵页、能看单词列表（读本地数据）
- [ ] 取消 Offline → 同步自动恢复
- [ ] 改一行代码重新部署 → 打开旧页面 → 出现「有新版本可用，点击刷新」提示条
- [ ] 真机：Android Chrome 能「添加到主屏幕」，点开无地址栏
- [ ] 真机：iPhone Safari 能「添加到主屏幕」，图标是白纸图标不是网页截图

## 五、iOS 语音自检（真机）

- [ ] 首次进背诵页出现「点击屏幕开始」引导层
- [ ] 点一下之后，新出现的单词能朗读出声
- [ ] 侧边静音键打开时听不到声音是正常的（引导层里已提醒）
- [ ] 第二次进入不再出现引导层

## 六、安全自检（代码 + 配置）

- [ ] `ALLOWED_ORIGIN` 填的是自己的域名，**不是 `*`**
- [ ] `AI_ALLOWED_HOSTS` 填的是实际用的 AI 域名（不是留空）
- [ ] 全库搜索确认：没有任何「存储 AI 密钥」的逻辑
- [ ] `api/ai-proxy.ts` **没有**连接数据库（文件里没有 `_lib/db`）
- [ ] 数据库里没有存密钥的表（只有 `words` 与 `sources`）
- [ ] 服务器日志里搜不到密钥明文（手动发几次请求，去 Vercel 日志里搜 `sk-`）
- [ ] 用非白名单 Origin 请求 `/api/ai-proxy` → 403
- [ ] 数据表主键是 `(space_key, id)`（不是单独的 `id`，否则跨空间会互相覆盖）

## 七、同步接口自检

> 最快的方式：跑 `npm run test:live`（自动读前端的路由表，按真实 URL 打一遍线上后端，
> 覆盖下面全部条目，跑完自动清理测试数据）。手测也可以，按下面的路径来。

- [ ] `GET /api/health` → `{"ok":true,"db":"connected"}`
- [ ] `GET /api/sync-pull` 不带 `X-Space-Key` → 401
- [ ] 带非法 spaceKey（如 `abc`）→ 401
- [ ] 用两个不同 spaceKey 拉取 → 数据互不可见
- [ ] 一次推 600 条 → 400，提示「单批不得超过 500 条」
- [ ] 推一条 `deleted=1` 的记录 → pull 仍然能拉到它（客户端靠它同步删除）
- [ ] 前后端路径一致：`/api/sync-pull`、`/api/sync-push`、`/api/sync-purge`（**连字符，不是斜杠**）

## 八、备份自检（**最重要**）

- [ ] 设置 → G 区「导出备份」能下载到 json，文件里词条数量对得上
- [ ] 同步码已经记在别的地方（手机备忘录 / 密码管理器）
- [ ] Chrome / Edge：连接本地文件夹后，改一个词，2 秒后 `blank-sheet-vocab-data.json` 内容变了
- [ ] 换一台设备：只靠同步码 + 导出备份，能把数据恢复回来

## 九、手动触发错误边界

- [ ] F12 控制台执行 `throw new Error('test')` → 出现「出问题了，但你的数据还在」兜底页，**没有白屏**
- [ ] 点「导出数据」能下载到 `blank-sheet-vocab-rescue-*.json`
- [ ] 点「返回首页」能回到首页继续用

---

## 已知限制（提前知道，别当成 bug）

- AI 密钥存在浏览器 localStorage，清缓存会丢；导出备份**不包含**密钥，换设备要重填
- 本地文件夹自动备份只有 Chrome / Edge 支持；Safari 降级成手动导出
- 音标与例句第一版靠 AI 补，规则解析模式下这两项为空
- 词量上万时列表筛选是内存过滤，会卡（代码里已注明改造方向）
- 删除用的是软删除墓碑（保证多设备一致），墓碑会一直留着，不占多少空间但不会自动清
- Vercel 的 `*.vercel.app` 域名在国内可能访问慢——但本项目本地优先，慢不影响背单词
- 同步冲突策略是「后写覆盖」：两台设备改同一个词、又刚好离线各自改，后同步的会覆盖前面的
