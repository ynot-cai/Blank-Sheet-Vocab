# ─────────────────────────────────────────────────────────────
# 后端（api/ 里的 Vercel Serverless Functions）用的环境变量
#
# 本地开发：复制成 .env.local 填真实值（.env.local 已被 .gitignore 忽略）
# 线上部署：在 Vercel 项目 → Settings → Environment Variables 里填，填完必须重新部署
#
# 前端代码**不读**这里的任何变量：AI 的「地址 / 模型 / 密钥」三项由用户
# 在自己的浏览器里填，只存在本机，不上传也不同步（方案 B）。
# ─────────────────────────────────────────────────────────────

# Turso 数据库地址。线上填 libsql:// 或 https:// 开头的地址；
# 本地想先跑通、暂时不注册 Turso 时，可以留成下面这个本地文件地址（数据落在 .tmp/dev.db）。
TURSO_DATABASE_URL=file:./.tmp/dev.db

# Turso 的访问令牌（Turso 控制台 → 数据库 → Create Token）。
# 本地用 file: 数据库时留空即可；线上是必填的。
TURSO_AUTH_TOKEN=

# 允许访问 API 的前端来源，逗号分隔可填多个。**不要用 `*`**。
# 本地开发默认 http://localhost:5173；线上必须改成你的 Vercel 域名，
# 例如 https://wordpaper.vercel.app（自定义域名也一起加上）。
ALLOWED_ORIGIN=http://localhost:5173

# AI 代理允许转发的「上游主机」白名单，逗号分隔，`*` = 不限制。
# 填上它，别人的网页就没法拿你的代理去请求任意地址（防 SSRF）；建议填你实际用的 AI 域名。
# 留空 / 不设置 = `*`（本地开发默认，方便对着本机假上游做测试）。
AI_ALLOWED_HOSTS=api.deepseek.com
