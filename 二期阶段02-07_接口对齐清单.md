# 二期阶段 02~07 跨阶段接口对齐清单

> 用途：**在动手写阶段 02 之前**，先把阶段 02~07 要用到的接口与阶段 01 的实际实现核一遍。
> 结论：**19 处依赖已就位，5 处冲突需要先定调，3 个新文件/路由属于后续阶段自己要建的**。
> 本文档只记录事实与建议，**不改任何阶段提示词原文**。

---

## 1. 已就位（后续阶段可以直接用，无需改动）

| 后续阶段依赖 | 阶段 01 实际提供 | 位置 |
|---|---|---|
| 02：`chatComplete(cfg, messages, {jsonMode, timeoutMs})` | 一期已有，签名完全对得上（含直连/代理自动切换、JSON 模式被拒自动降级） | `services/ai.ts:263` |
| 02：`AiConfig` / `aiConfigFromSettings(settings)` | 一期已有 | `services/ai.ts:23 / 97` |
| 02：`validateCard` / `validateBlock` / `sanitizeText` | 有（`kcModel` 统一出口） | `core/kcModel.ts` |
| 02：`dao.kc.bulkUpsert` | 有，返回 `{inserted, updated}` | `dao/kc.ts` |
| 02：AI 超时 90 秒 | `chatComplete` 接受 `timeoutMs` 覆盖默认值 | `services/ai.ts:272` |
| 03：`renderBlocks`（预览用） | 有，返回 `DocumentFragment` | `core/blockRender.ts` |
| 03：`EXAM_TYPES`（标签多选） | 有，含 `findExamType` / `filterExamTags` | `core/kcTypes.ts` |
| 03：`dao.kc.updateBlocks(id, blocks)` | 有（卡片不存在返回 `false`，不抛异常） | `dao/kc.ts` |
| 03：`dao.kc.query(q)`（搜索/筛选/排序/分页） | 有，纯函数实现在 `dao/kcQuery.ts`，**已单测 17 项** | `dao/kcQuery.ts` |
| 03：`dao.kc.stats()`（顶部统计） | 有 | `dao/kc.ts` |
| 03：斩 / 复活 / 「已斩」筛选 | 有（`chop` / `revive` + `query({status:['chopped']})`） | `dao/kc.ts` |
| 03：`dao.kc.updateExamTags(id, tags)` | 有 | `dao/kc.ts` |
| 04：`dao.kc.updateAttrs(id, patch)` | 有，**会自动重算 `mastery` + `reviewPriority`** | `dao/kc.ts` |
| 04：掌握度算法 `calcMastery` | 有（含 16 格对照表注释） | `core/kcMastery.ts` |
| 04：`reviewPriority` 计算 | 有（`computeKcPriority`，含方向惩罚与分数差距项） | `core/kcPriority.ts` |
| 04：KcSession 里 `examScores` 字段 | 阶段 01 **刻意没建**会话表，等 04 建（见 §2.2） | — |
| 05：语境词「近 30 天 / 近 3 天」检索 | 有：`contextWords.recentWords()` / `examBank.recentQuestions()` | `dao/contextWords.ts` / `examBank.ts` |
| 05：题库样题随机抽取 | 有：`examBank.listBankQuestions(type)` | `dao/examBank.ts` |
| 05/06：云同步通道 + spaceKey 隔离 | 有：`kcSyncOnce`（二期独立游标）+ 4 张表都带 `space_key` | `dao/kcCloud.ts` |
| 06：`settings.kc.reviewWordLimit`（默认 5） | 有 | `core/config.ts` |
| 06：一期词源 `dao.words` | 有 `listAlive()` / `stats()` / `applyPriorities()` | `dao/words.ts` |

---

## 2. ★ 5 处冲突（动手前需要定调）

### 2.1 路由不能用 `/kc/edit/:id`（阶段 03 §5）

**问题**：阶段 03 写的是 `#/kc/edit/:id`，但一期路由是**精确匹配的 Map**（`router.ts:16`），
没有动态段解析。现有带参数的页面（`/learn`、`/review`）都是**读 session 或 query**。

**建议**：改成 `#/kc/edit?id=<cardId>`（query 形式），与一期 `/learn?type=...` 同款。
`RouteContext` 已经提供 `ctx.query: URLSearchParams`，零成本。
二期路由表建议：

```
#/kc            #/kc/import      #/kc/list      #/kc/edit?id=<id>
#/kc/study      #/kc/review      #/kc/settings  #/kc/bank
```

### 2.2 阶段 04 要求新增 `dao/kcSession.ts`（阶段 01 没建）

阶段 01 建了 IndexedDB 的 4 张二期表（卡片/语境词/题目历史/题库），**没有会话表** —— 这是有意的：
阶段 01 的验收标准里没有会话，而 `KcSession` 的结构由阶段 04 定义。
**阶段 04 需要**：
- IndexedDB 加第 9 张表（`kcSessions`）→ **库版本 v3 → v4**（又是一次纯加法升级）；
- `dao/kcSession.ts`：`save / load / clear`；
- 「保存并退出」的恢复逻辑。

> 提前打招呼的原因：**库版本升级要小心**。v4 升级会再触发一次 `onupgradeneeded`，
> 已有的 8 张表必须原样保留（用 `contains()` 判断，和 v3 同款写法）。这条我会加测试守着。

### 2.3 ALL 阶段要求新建的 3 个 API 路由（阶段 01 只做了 2 个）

阶段 05 与主提示词第 6 节都点名了这些接口，但**阶段 01 的任务清单里只有 `kc-list` / `kc-push`**：

| 路由 | 点名位置 | 阶段 01 状态 | 谁建 |
|---|---|---|---|
| `api/kc-list.ts` | 主提示词 §6 | ✅ 已建 | — |
| `api/kc-push.ts` | 主提示词 §6 | ✅ 已建 | — |
| `api/context-words.ts` | 阶段 05 §1 | ❌ 未建 | 阶段 05 |
| `api/exam-history.ts` | 主提示词 §6 | ❌ 未建 | 阶段 05 |
| `api/bank-questions.ts` | 主提示词 §6 | ❌ 未建 | 阶段 05 或 07 |

**这不影响阶段 01 的验收**（它没要求），但意味着：
- 阶段 05 除了出题/评分，还要**顺手建 3 个接口 + 给 `exam_records` / `bank_questions` 加同步**；
- `bank_questions` 表**没有 `updated_at` 列**（按你给的建表语句），没法做后写覆盖。
  要同步它就得补列（`ALTER TABLE ADD COLUMN` 是幂等的，Turso 支持）。

**建议**：阶段 05 拆两半，或者把「三张表的同步」整体挪到阶段 07 和数据管理一起做。
**现在只需要你拍一句：阶段 05 是否包含这 3 个接口？**

### 2.4 `contextWords` 的函数名与签名不一致（阶段 05 §1）

| 阶段 05 写的 | 阶段 01 实际有 | 建议 |
|---|---|---|
| `getTodayWords()` | `getForDate(date?)` | 加一个别名 `getTodayWords()`（一行），或直接用 `getForDate()` |
| `generateTodayWords(cfg)` | 无（AI 调用属于阶段 05） | 阶段 05 新建，内部用 `save()` 落库 |
| `confirmTodayWords(words)` | `confirm(id, words?)` | 阶段 05 包一层（要先把当天那行取出来拿 id） |

阶段 01 的 DAO 已经把**纯数据部分**做完了（去重、数量截断、自然日、`confirmed` 标记、
近 N 天检索），阶段 05 只需要在上面加「AI 生成」与「三个对外函数」。

### 2.5 `pickForReview` 一期已经有一个同名函数（阶段 06 §2）

一期 `core/pick.ts:93` 有 `pickForReview(words: Word[], count, settings, now)` —— **签名与阶段 06 要的几乎一样**
（候选过滤条件、降序、同分时 `lastReviewAt` 升序，全都一致），只是对象从 `Word` 换成 `KnowledgeCard`。

**建议**：二期版本放 `core/kcPick.ts`（或在 `kcPriority.ts` 里），**不要改一期那个**（改了要动一期的复习流程）。
共用的排序想法可以照抄，但实现分开——这是一期二期「数据完全独立」的直接后果。

---

## 3. 轻微不一致（不阻塞，动手时顺手对齐）

| # | 位置 | 阶段提示词写的 | 实际/建议 |
|---|---|---|---|
| 1 | 阶段 02 §3 | 「已有标题列表从 `dao.kc.getAll()` 取」 | `getAll()` **含墓碑**（同步要用）。给 AI 的列表应改成 `getAll().filter(c => c.deleted !== 1)`，否则已斩的卡片标题会让 AI 误判「已有相似卡片」 |
| 2 | 阶段 02 验收 6 | `examLoad.estMinutes` 在 3~5 | 提示词里写了「控制在 3~5 分钟」，但**代码层没有钳制**。建议在 `kcAi` 解析后统一钳到 `[3,5]`（AI 不一定听话），并把范围放进 `KC` 常量表 |
| 3 | 阶段 03 §3 | 搜索「标题 + 摘要 + **块内容**」 | 阶段 01 的 `filterCards` 只搜标题+摘要（当时判断正文噪音大）。要按阶段 03 改：**加进块内容**，但那会让「搜『的』命中所有卡」。建议：块内容匹配**降权**或只匹配 `heading`/`text` 块 |
| 4 | 阶段 03 §2 | `examLoad.estMinutes` 输入范围 3~5 | 与 #2 同一个常量，一起做 |
| 5 | 阶段 07 §1.1 | 掌握度「三个数字输入 w1/w2/penalty」 | 阶段 01 为了满足**验收项 3** 多了一个 `asymmetry`（默认 2，方向惩罚）。设置页要不要暴露它？<br>**建议暴露**，并在说明里写清「=1 就退回对称公式（你原文的公式）」，否则用户改不回原公式 |
| 6 | 阶段 07 §1.2 | 复习优先度用**表达式**（`daysSinceReview * 0.5 + (1-mastery) * 10`） | 阶段 01 用的是固定加权公式（`masteryWeight`/`staleWeight`/`gapWeight`）。阶段 07 要换成表达式机制的话，**三个预设的表达式里用到的变量**（`daysSinceLearned`/`daysSinceReview`/`mastery`/`reviewCount`）需要在二期侧新建白名单，不能直接用一期 `priority.ALLOWED_VARS`（那套是单词的变量） |
| 7 | 阶段 05 §4 | 「允许手动改分」 | 改分后要**同时更新 `ExamRecord.aiScore` 与 `card.attrs.lastExamScore`**，并重算 mastery。这一点提示词没写全，阶段 05 记得补 |
| 8 | 阶段 06 §3 | 桥接「复用一期页面，不复制一套」 | 一期 `LearnPage` 的入口是 `/learn?...` + session。桥接要么用 `session.type='kc-review'` + 新路由参数，要么抽一个参数化的组件。**这是二期唯一会碰到一期核心的地方**，动手前我会先把一期 `paper/flow.ts` 的入参核清楚再定 |

---

## 4. 建议的执行顺序

主提示词第 9 条写的是「**一次只做一个阶段**」，每个阶段完成后停下等下一份提示词。
按这个节奏，下一步就是**只做阶段 02**。阶段 02 自己要产出的东西：

```
src/services/kcPrompts.ts     IMPORT_PROMPT（三套里先做第一套，另两套留 TODO）
src/services/kcAi.ts          analyzeWeakPoint()：调用 + 解析 + 逐块校验清洗
src/ui/pages/KcImportPage.ts  聊天界面（对话区 + 卡片预览 + 采纳/丢弃 + 确认入库）
src/App.ts / router          加 #/kc（占位首页）+ #/kc/import + 一期主界面「知识点」入口
src/styles/kc.css            二期页面样式（阶段 01 只定义了块样式）
```

**阶段 02 会第一次需要 AI 密钥**（验收标准 2、3、4 都要真跑一次 AI）。
项目是「方案 B：密钥只存浏览器」，所以：
- 我可以在没有密钥的情况下把代码写完 + 用**假响应**验掉验收项 4、9（XSS 与非法 JSON）；
- 但验收项 2、6、8（真的拆出 ≥2 张卡、耗时 3~5 分钟、不重复生成）**需要你提供密钥**，
  或者由你在浏览器里跑一次把结果贴给我。
