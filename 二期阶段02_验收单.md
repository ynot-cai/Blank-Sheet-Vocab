# 二期阶段 02 验收单（聊天式录入）

> 交付：`#/kc/import` 聊天式录入 —— 说一句「我这块不行」，AI 拆成结构化的知识点卡片，
> 逐张预览 / 采纳 / 编辑 / 丢弃，最后一次性入库。
> 代码质量：`npx tsc --noEmit` 与 `npm run build` 零报错；`npm test` 全量回归通过。

---

## 1. 验收标准逐条对照

| # | 你的验收标准 | 结论 | 怎么自己核 |
|---|---|---|---|
| 1 | `npx tsc --noEmit` 零报错、`npm run build` 通过 | ✅ | 两条命令 |
| 2 | 输入「我在定语从句这块不行」→ AI 返回 ≥2 张卡，每张结构完整 | ⏳ **待你真跑** | 见 §3；解析链路已用固定 AI 返回验过（`test:kc-import` + `test:kc-import-ui`） |
| 3 | 卡片预览能正确渲染各种块，排版正常不白屏 | ✅ | `npm run test:kc-import-ui`：真 Chrome 里弹出 8 种块，表格是真 `<table>`（3 表头）、列表是 `<li>`、代码是 `<pre>` |
| 4 | **XSS**：AI 返回含 `<script>` 的内容 → 纯文本、不执行 | ✅ | 同上一行：页面里 `script/img/svg/iframe` = 0 个、`onerror/onload` 属性 = 0 个、哨兵 `__kcXssFired` 未置位、载荷以纯文本可见 |
| 5 | 考核标签正确显示（fill + choice） | ✅ | 界面显示「语法填空」「选择题」两个 chip；未知题型 `essay` 被过滤掉 |
| 6 | `examLoad.estMinutes` 在 3~5 之间 | ✅ | 假 AI 故意给 12 → 界面显示「约 5 分钟」（钳到上限）；给 3 的保持不变 |
| 7 | 采纳 → 确认入库 → 列表/数据库能看到、字段完整 | ✅ | 真浏览器里点完两个按钮，查 IndexedDB：2 张卡、`status=unlearned`、9 个块、2 个标签、耗时 5、`mastery=0` |
| 8 | 不重复生成（传了 existingTitles） | ✅ | 已验「已有标题真的进了提示词」+「已斩卡片的标题会被滤掉」；真实去重效果见 §3 |
| 9 | AI 返回非法 JSON 时有明确提示、不崩溃 | ✅ | 三种降级都测了：夹在解释文字里的 JSON（抠出来）、markdown 围栏（剥掉）、纯废话（报错 + 重试按钮 + 页面不崩） |
| 10 | 一期功能完好 | ✅ | `npm test` 全量（一期 8 个套件全绿） |

**用例数**：`test:kc-import` **58 项**（解析层，不需要密钥）、`test:kc-import-ui` **36 项**（真 Chrome）。

---

## 2. 新增文件

| 文件 | 作用 |
|---|---|
| `src/services/kcPrompts.ts` | 三套提示词的集中管理处。录入提示词完整实现；出题/评分留 `TODO` 常量（阶段 05 填） |
| `src/services/kcImportParse.ts` | **解析层**：JSON 抽取与三级降级、逐块校验清洗、题型白名单、耗时钳制 |
| `src/services/kcAi.ts` | **调用层**：`analyzeWeakPoint()`，复用一期 `chatComplete`（含直连/代理自动切换、JSON 模式降级） |
| `src/ui/components/KcChatPanel.ts` | 聊天面板：会话状态机 + 渲染（消息、采纳状态、用户编辑覆盖、忙碌/取消） |
| `src/ui/components/KcCardPreview.ts` | 卡片预览组件（标题/摘要/chip/耗时 + 块预览 + 采纳/编辑/丢弃） |
| `src/ui/components/KcMetaEditor.ts` | 「改标题 / 摘要 / 考法标签」弹窗（完整块编辑是阶段 03 的 `BlockEditor`） |
| `src/ui/pages/KcImportPage.ts` | 录入页：布局 + 发起请求 + 入库 + 手动新建兜底 |
| `src/ui/pages/KcHomePage.ts` | 二期首页（6 个入口，本期只「录入」可用） |
| `api/_dev/kcImportFixture.mjs` | 假 AI 返回（Node 与浏览器测试共用同一份） |
| `api/_dev/test-kc-import.mjs` / `test-kc-import-ui.mjs` | 两套验收脚本 |

**改动的既有文件**：`src/App.ts`（两条二期路由 + 顶栏「知识点」入口）、`src/styles/kc.css`（二期页面样式）、
`src/core/config.ts` + `kcTypes.ts`（`examLoadMin/MaxMinutes`、`ParsedKcCard`）、`api/_dev/test-build.mjs`（新增 BOM 护栏）、`package.json`。

---

## 3. 只差你那一步：真跑一次 AI（验收标准 2 / 8）

阶段 02 的解析链路已经被**固定 AI 返回**完整验过（36 项真浏览器用例），
所以只差「真实模型产出的内容质量」。请你这样跑：

```
1. git push（或本地 npm run dev）
2. 打开页面 → 设置 → B 区填 AI 接口地址 / 模型 / 密钥
   （也可以在设置页点「测试连接」先确认通）
3. 顶栏点「知识点」→「录入」
4. 输入：我在定语从句这块不行   → 发送
5. 把结果贴给我，我需要这几个数：
   - 拆出了几张卡？（期望 2~5）
   - 每张卡有几个块、都是什么类型？（期望 block 类型有变化，不是清一色 text）
   - 标签是什么？（期望 1~3 种，如 fill + choice）
   - 耗时显示几分钟？（期望 3~5）
   - 一次调用的耗时大约多少秒？（期望 < 90 秒）
6. 再发一次**同一个问题**，看 AI 会不会因为「已有标题」而少生成或提示重合（验收标准 8）
```

如果 AI 输出质量不理想（比如卡片太碎、块类型单一、例句不地道），
**改 `src/services/kcPrompts.ts` 里的提示词即可**，不用动其它文件 —— 这是把提示词集中管理的意义。

---

## 4. ★ 三处与提示词原文不同，需要你确认

### 4.1 「采纳」不立刻落库，而是进「待入库区」

提示词写的是「已采纳的卡片要落库」。我改成了：**采纳 = 进待入库区，点「确认入库（N 张）」才写库**。

**为什么**：如果采纳即落库，「丢弃」就形同虚设（用户以为丢了，其实库里已经有了）；
而且中途关掉页面会留下一堆只有标题、没编辑过的半成品。现在这样「确认入库」是一次明确的动作，
配一个 Toast + 「继续录入 / 回首页」的询问，和提示词里第 5 条的流程也更贴近。

**代价**：用户中途离开，已采纳但没确认入库的卡片会丢（对话历史本来就不落库）。
**如果你更想要「采纳即落库」，说一声，改动很小。**

### 4.2 路由形式：`#/kc/import`（不是 `/kc/import/:id` 那类带路径参数的写法）

一期的路由是精确匹配的 Map，没有动态段。阶段 03 要的 `#/kc/edit/:id` 会改成 `#/kc/edit?id=<id>`，
这一点在我上一轮给你的《接口对齐清单》里已经说过；本阶段用到的两条路由都是静态的，不受影响。

### 4.3 缺 `type` 的块会被降级成 `text` 并**保留**

场景：AI 偶尔会漏写 `type`（比如只给 `{"content":"..."}`）。
我的处理是**降级成 text 段落并保留内容**，而不是丢掉。理由：内容是知识，丢了就没了；
而真正被丢掉的只有「根本不是对象」的项（`null` / 字符串 / 数字）。

这条有测试盯着：假 AI 返回里故意放了一个 `{"content":"<img src=x onerror=...>"}`，
断言它被保留成 text 块、而且**渲染出来是纯文本**（`img` 元素数 = 0）。

---

## 5. 顺手修掉的一个**高危**事故（建议你重点看一眼）

**我自己在上一阶段埋的雷，这轮排掉了。**

**症状**：`npm run dev` 打开页面**全白**，控制台只有几条 `500 (Internal Server Error)` 指向 `src/styles/*.css`。

**原因**：我用 PowerShell 的 `Set-Content -Encoding UTF8` 改过 `package.json`，
Windows PowerShell 会在文件开头写一个 **UTF-8 BOM**（`EF BB BF`）。
后果极其阴险：

| 工具 | 对 BOM 的态度 | 结果 |
|---|---|---|
| `node`（读 JSON） | **容忍** | 所有 `npm test` 套件**全绿** |
| `tsc` | 容忍 | 类型检查**全绿** |
| `vite build` | 不走那条路径 | `npm run build` **也绿** |
| **`vite dev` 的 PostCSS 配置加载器** | 用 `JSON.parse` **严格**解析 `package.json` | 抛 `Unexpected token '锘?'` → 所有 CSS 请求 500 → `main.ts` 的 `import './styles/global.css'` 挂掉 → `boot()` 根本不执行 → **整页白屏** |

也就是说：**四道测试全绿，但应用起不来。**

**修复**：
1. 剥掉 BOM —— 我自己造成的 5 个（`package.json` + 4 个新建文件）+ 仓库里**本来就带着**的 2 个
   （`CloudSection.ts` / `DataSection.ts`，git 里就有 BOM，之前没人发现）；
2. `api/_dev/test-build.mjs` 新增 **`[9] UTF-8 BOM 护栏`**：扫 `src/` `api/` 与所有配置文件，
   有 BOM 就红；另加一条「`package.json` 必须能被严格 `JSON.parse`」。
   **这个护栏以后会替我们挡住这类问题。**

> ⚠️ 如果你也在这个项目里用 PowerShell 改文件：**不要用 `Set-Content -Encoding UTF8`**，
> 用 `-Encoding utf8NoBOM`（PowerShell 7+）或干脆用编辑器。`npm test` 会立刻告诉你有没有踩。

---

## 6. 本阶段没做的事

- **卡片列表 / 编辑页 / 学习 / 复习 / 题库 / 设置**：首页有入口按钮，点了会明确提示「等阶段 0X」。
  完整块编辑器（增删改拖、表格增删行列）是**阶段 03**。
- **「编辑」弹窗只改标题 / 摘要 / 考法标签**：块内容的编辑要等阶段 03 的 `BlockEditor`
  （提示词里也写了「本阶段可先只改标题/标签」，一致）。
- **对话历史不落库**：刷新即清（提示词明确要求如此）。
- **没有真跑过真实模型**：见 §3。
- **`api/context-words.ts` 等 3 个接口**：按你的决定，留到阶段 05 一起做。
