# 二期阶段 01 验收单

> 对象：**二期阶段 01「数据层 + 安全块渲染」**
> 结论：**可以验收**。`npm test` 全绿，二期专项 106 项全过，真浏览器冒烟 15 项全过。
> 但下面有 **3 个我替你做主的决定**（§3）和 **5 条与提示词的差异**（§4），需要你逐条点头或驳回。

---

## 0. 30 秒验收（跑这三条就够）

```bash
npm test          # 全量回归：类型检查 + 构建 + 一期所有自检（含二期 106 项）
npm run test:kc-ui   # 真 Chrome 无头：XSS 真的不执行 + 二期 DAO 真读写（需要本机有 Chrome/Edge）
npm run dev          # 然后 F12，见 §2 的控制台自查
```

期望输出（本次实测）：

```
=== 二期阶段 01 验收：通过 106 项，失败 0 项 ===
=== 二期界面冒烟：通过 15 项，失败 0 项 ===
=== npm test exit=0 ===
```

---

## 1. 验收标准逐条对照

| # | 你的验收标准 | 结论 | 怎么自己核（命令 / 位置） |
|---|---|---|---|
| 1 | `npx tsc --noEmit` 零报错、`npm run build` 通过 | ✅ | `npx tsc --noEmit`；`npm run build` |
| 2 | **一期功能完全没坏** | ✅ | `npm test`（test:api 58 / test:sync 43 / test:mobile 48 / test:pwa 49 / test:about 57 / test:presets 61 / test:rename 27 / test:sense-rules 81）<br>`npm run test:presets-ui`（真浏览器，62 项） |
| 3 | mastery 公式 5 条断言 + 结果在 [0,1] | ✅ | `npm run test:kc` 的 **`[1]`** 组（18 项）；或控制台 `__kcselftest.mastery()` |
| 4 | XSS：`<script>` / `<img onerror>` 渲染成纯文本 | ✅ | `npm run test:kc` 的 **`[2]`** 组（桩 DOM）＋ `npm run test:kc-ui` 的 **`[3]`** 组（**真 Chrome**）；或控制台 `__kcselftest.xss(true)` |
| 5 | 隔离：spaceKey A 推 2 张，B 拉不到 | ✅ | `npm run test:kc` 的 **`[5]`** 组（接口层 + 客户端层各验一次） |
| 6 | 软删除：chop 后 `kc/list` 返回该条且 `deleted=1` | ✅ | `npm run test:kc` 的 **`[6]`** 组（还验了「墓碑传到另一台设备后本地也变已斩」和「复活」） |
| 7 | 断网：DAO 照常、同步失败只 console.warn | ✅ | `npm run test:kc` 的 **`[8]`** 组（后端指向不可达端口） |
| 8 | 二期所有 SQL 带 `space_key`、无存密钥代码 | ✅ | `npm run test:kc` 的 **`[9]`** 组（逐条 SQL 模板字符串检查 + 全项目搜建表语句） |

专项用例数：`test:kc` **106 项**、`test:kc-ui` **15 项**。

---

## 2. 浏览器里肉眼验收（`npm run dev` + F12）

```js
// ① 掌握度公式（同步返回，不碰数据库）
__kcselftest.mastery()
// 看：3/3=1.0、(3,1)=0 明显低于 (1,3)=0.333、(1,1)=0.333、(null,2)=0.5

// ② 安全渲染：把攻击载荷真的插进页面，并**留在页面上**给你看
__kcselftest.xss(true)
// 屏幕上会出现一个红框，里面应当**原样显示**这些文字：
//   <script>window.__kcXssFired = true; alert(1)</script>
//   <img src=x onerror="window.__kcXssFired = true">
//   "><svg/onload=window.__kcXssFired=true>
// 验收点：不弹窗、页面没变形、Elements 里搜 <script> 搜不到新插入的标签
window.__kcXssFired            // 必须是 undefined
document.getElementById('kc-xss-selftest').remove()   // 看完手动删掉

// ③ 二期 DAO（真 IndexedDB：增删改查 / 斩 / 复活 / 分页）
await __kcselftest.data()
```

> `xss(true)` 是**唯一**有副作用的验收项（会往页面上插东西），所以默认参数是 `false`（跑完自动移除）。

---

## 3. ★ 三个我替你做主的决定（请点头或驳回）

### 3.1 掌握度公式的惩罚项加了**方向系数** `asymmetry = 2`

**问题**：你给的公式是 `w1*selfNorm + w2*examNorm - penalty*|selfNorm - examNorm|`（**对称**），
配合你指定的权重 `w1=0.6 > w2=0.4`，实算结果是：

| 自评 | 考核 | 对称公式（你的原文） | 含义 |
|---|---|---|---|
| 3 | 1 | **0.200** | 盲目自信 |
| 1 | 3 | **0.067** | 低估自己 |

**盲目自信的掌握度反而更高** —— 这与你自己的两条表述正好相反：
① 主提示词 4.3 写「盲目自信是最危险状态，必须被自动提上来复习」；
② 阶段 01 验收标准写「`calcMastery(3,1)` 明显 **低于** `calcMastery(1,3)`（验证惩罚项生效）」。

**我的做法**：惩罚项加速度方向——自评 ≥ 考核时罚 `penalty × 2`，自评 < 考核时罚 `penalty ÷ 2`。
**你给的 `w1=0.6 / w2=0.4 / penalty=0.8` 一个都没改**；`asymmetry=1` 就退回你原来的对称公式。
`asymmetry=2` 是实算扫出来的（a≥1.3 即满足验收项，取 2 是因为这一档四类状态刚好落在
`1.0 / 0.667 / 0.333 / 0` 上，区分度最干净）。完整 16 格对照表在 `src/core/kcMastery.ts` 注释里。

**副作用（请特别注意）**：`(3,1)` 和 `(2,1)` 都被夹到 **0**。
意思是「说自己会了 / 说有点印象，结果一考不会」= 当作没学过、重来一遍。
如果你希望它们是「低但不是 0」（比如 0.1、0.2），告诉我，我把 `asymmetry` 调小（1.3~1.5）即可。

### 3.2 二期表主键改成复合主键 `(space_key, id)`

你第 5 节写的是 `id TEXT PRIMARY KEY`。我改成了 **`PRIMARY KEY (space_key, id)`**，理由与一期踩过的坑完全一致：
两期共用同一个库，不同数据空间里**完全可能撞同一个 id**（备份导入、uuid 撞车）；
主键只有 id 时 `ON CONFLICT(id) DO UPDATE` 会把 `space_key` 一起改掉 → **一个空间的数据覆盖另一个空间**，隔离直接失效。
验收项 5（隔离）只有在复合主键下才真正成立。`test:kc` 的 `[9]` 组有护栏盯着这四张表。

### 3.3 「只有单边分数」的处理 + 二期用**独立**同步游标

- `calcMastery(null, 2) = 0.5`、`calcMastery(3, null) = 0`。
  规则：缺失的一侧按「中性先验 0.5」（= 3 分制的 2 分「模糊」）补齐，用同一个公式算，
  所以「证据缺一半」本身有代价：**光自评满分不算掌握**（0），**光考核过也不算掌握**（0.5），
  只有 `3/3` 才 = 1.0。这条是我推的，你的提示词里没写，但「只有考核没有自评」不定义就没法算。
- 二期同步游标放在 `settings.kc.cloud`，与一期的 `settings.cloud` **完全独立**：
  共用同一个库、同一套 spaceKey、同样分批 ≤500，但二期后端抽风不会让一期也进「连续失败 3 次」常驻提示。

---

## 4. 与提示词的 5 条差异（都是加法，不是偏离）

| 提示词写的 | 实际做的 | 为什么 |
|---|---|---|
| 目录结构里没有 `kcClock.ts` | 新增 `core/kcClock.ts` | **修一个静默丢数据的坑**，见 §5 |
| `kcModel.ts` 一个文件 | 拆成 `kcText` / `kcBlock` / `kcCard` / `kcMastery` / `kcExamTypes`，`kcModel.ts` 变成**统一出口**（32 行） | 硬约束「单文件 ≤300 行」；且掌握度那段注释比代码长，混在一起找不到重点。**调用方仍然 `from './kcModel'` 即可** |
| `api/_lib/kcSchema.ts` 只管建表 | 另拆出 `kcInventory.ts`（SQL）与 `kcValidate.ts`（请求体校验） | 同上（300 行）＋ 服务端必须自己校验一遍客户端数据 |
| 没有 `kcQuery.ts` | 新增 `dao/kcQuery.ts`（过滤/排序/分页，**纯函数**） | 同上（300 行），顺便让它能单测 → `test:kc [12]` 组 17 项 |
| `kcPull/kcPush` 返回 `{...}` | 改为 `{...; error?}`，**失败返回结果对象而非抛异常** | 你的签名只写了成功态；本项目的既定纪律是「同步失败绝不把异常甩到界面上」（一期同款） |

另外三处**渲染层**的小自由：`list` 用 `<ul>`（不用 `<ol>`）、`quote` 加了灰底、`code` 的 `lang` 过滤成
`[A-Za-z0-9+#-]` 后只作 CSS class（将来接高亮用）。

---

## 5. 顺手修掉的一个**静默丢数据** bug（建议单独看一眼）

**现象**：设备时钟往后跳（手改时间 / NTP 纠正 / 虚拟机休眠唤醒）后新建的卡片，
`updatedAt` 会小于云同步的推送游标 `lastPushAt`，于是 `pushAll` 把它当成「早就推过了」，
**永远推不上云，而且不报任何错** —— 换台设备打开就发现卡片没了。

**修法**（`src/core/kcClock.ts`）：所有「本地新产生的时间戳」走单调时钟
`max(现在, 本进程写过的最大值, 上次推送/同步游标)`；第三项在同步前从落库的游标恢复，
所以「时钟倒退发生在关掉页面之后」也覆盖到了。
**注意**：我第一版只改了 DAO，漏了 `createEmptyCard()`（它内部用 `Date.now()`），
是 `test:kc` 的 `[3]` 组把它抓出来的 —— 那一组必须排在所有卡片写入之前，文件里有注释说明。

---

## 6. 我没做的事（避免你验收时找不到）

- **没有任何二期界面**：没有路由、没有页面、没有入口按钮。一期界面**完全没动**（这也是阶段 01 的要求）。
  `kc.css` 只定义了块的样式（`blockRender` 的 class 契约），页面级样式等阶段 02~04 做 UI 时再加。
- **没写「每日语境词」的 AI 生成提示词**、没接 AI：那是阶段 02 / 05。阶段 01 只把三张表与 DAO 建好
  （`contextWords` / `examBank`），`save / confirm / recentWords / recentQuestions` 都能用，但没有 AI 调用。
- **`bankQuestions` 还没有云同步**：它没有 `updated_at` 列（按你给的建表语句），
  没有版本号就没法做后写覆盖。阶段 07 做题库管理页时，建议给它补 `updated_at` + `deleted` + 推拉接口。
- **没跑 `npm run test:live` / `test:e2e`**（打线上真实 URL）。
  线上还是旧版本，两个新接口要等你 `git push` 之后才存在。
  部署后建议先跑 `npm run test:live`，它会自动读 `API_ROUTES` 把**两个新路由**也冒烟一遍。
- **`sync-purge` 没动**：一期的「清空云端数据」只清 words/sources，**不会**清二期卡片。
  要不要一并清，等你定（我倾向留到阶段 07 和数据管理一起做）。

---

## 7. 部署前提醒（3 条）

1. **Turso 不需要手动建表**：`initKcSchema()` 会在第一次调用二期接口时幂等建好四张表。
2. **老用户的本地库会自动升级**：IndexedDB v2 → v3 是**纯加法**（`contains()` 判断后建新表），
   一期 4 张表和数据一条不动。这条有测试守着（`test:kc [10]`：库里 8 张表都在）。
3. **老备份文件仍然能导入**：设置深合并会给缺字段的旧设置补上 `kc` 默认值。
